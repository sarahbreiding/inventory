<h1>All Categories</h1>
<% @categories.each do |category| %>
<p><%= link_to category.name, category %><%= button_to "Edit", edit_category_path(category), method: :get %><%= button_to "Delete", category, method: :delete, data: {confirm: "Are you sure?"} %></p>
<% end %>


