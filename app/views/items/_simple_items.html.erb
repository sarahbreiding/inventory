<% @items.each do |item| %>
  <ul>
    <li>
      <%= item.name %>
      (<%= item.quantity || 0 %>)
      <%= item.notes %>
      <%= link_to image_tag("Edit.png"), edit_item_path(item) %>
    </li>
  </ul>
<% end %>
